// project open + launch.json editor — Phase 1 track C
(function () {
  // Placeholder: this track owns opening a project and editing its launch.json
  // from the HUD. Nothing to initialise yet.
})();
